pipeline {
    agent any
    stages {
        stage (sourceCode) {
            steps {
                echo 'checkout sourceCode'
            }
            
        }
        stage (build) {
            steps {
                echo 'build project'
            }
            
        }
    }
}